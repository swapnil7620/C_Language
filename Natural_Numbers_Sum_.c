// print the sum of n natural number  1 to n

/*
#include <stdio.h>

void main()
{
    int Num;
    int Sum = 0 ;
    printf("Enter the Number ");
    scanf(" %d", &Num);
    for (int i = 0; i <=Num; ++i)
    {
      //   Sum = Sum + i;
       Sum += i;

    }
    printf("%d\n", Sum);
}

 */